{% extends "base.html" %}
{% block content %}
	<h1>{{ pagetitle }}</h1>
	<p>{{ contentbody }}</p>
	{% if local %}
	<ul>
		<li>
		<p>Descripcio: {{ local.descripcio }}</p>		
		<p>Telefon: {{ local.telf }}</p>
		<p>Email: {{ local.email }}</p>
		<p>Ciutat: {{ local.ciutat }}</p>
		<p>Carrer: {{ local.carrer }}</p> <!-- aixo es clau forana -->
		<p>Festes:</p>
			<ul>
				{% for festa in festes %}
				<li>
				<p><a href="/festes/{{festa.id}}"<h3>{{festa.titol}}</h3></a></p>
		<!-- 	
				<p>{{ festa.descripcio}}</p> 
				<p>{{ festa.local.nom}}</p>
				<p>Data: {{ festa.data }}</p>
				<img src="../../media/{{ festa.imatge }}">
		-->		
				</li>
				{% endfor %}

			</ul>  
		<!-- si volem fikar imatge... <img src="../../media/{{ local.imatge }}">-->
		</li>

		

	</ul>
	<p><a href="/locals">TORNA A LA LLISTA</a></p>
	{% else %}
	<p>No Party</p>
	{% endif %}
{% endblock %}
